(function(){
  const d=RENT_DATA, A=RentAnalysis, rows=d.rentals, u=A.union(rows), s=A.summarize(u);
  const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const usd=x=>x==null?'—':x.toLocaleString('en-US',{style:'currency',currency:'USD',minimumFractionDigits:2,maximumFractionDigits:2});
  const pct=x=>x.toFixed(2)+'%';
  const table=(headers,rs)=>'<table class="method-table"><thead><tr>'+headers.map(x=>'<th>'+x+'</th>').join('')+'</tr></thead><tbody>'+rs.map(r=>'<tr>'+r.map(x=>'<td>'+x+'</td>').join('')+'</tr>').join('')+'</tbody></table>';
  document.getElementById('sample-flow').innerHTML=`<strong>Inclusion flow:</strong> ${rows.length} saved IDs − ${rows.length-u.length} outside all three five-mile views = ${u.length} mapped IDs in the combined geography. Of those, ${s.excluded} prices are excluded, leaving ${s.perOverall.n+s.wholeOverall.n} usable prices: ${s.perOverall.n} room/per-bedroom + ${s.wholeOverall.n} whole-unit. Excluded reasons can overlap; count each excluded ID once.`;
  document.getElementById('worked-union').innerHTML=[['Room / per bedroom',s.perOverall],['Whole unit',s.wholeOverall]].map(([label,v])=>`<h2>${label}: combined five-mile sample</h2><ol><li>Add low prices: <strong>${usd(v.low_sum)}</strong>; divide by ${v.n} = <strong>${usd(v.avg_low)}</strong>.</li><li>Add high prices: <strong>${usd(v.high_sum)}</strong>; divide by ${v.n} = <strong>${usd(v.avg_high)}</strong>.</li><li>Mean midpoint = (${usd(v.low_sum)} ÷ ${v.n} + ${usd(v.high_sum)} ÷ ${v.n}) ÷ 2 = <strong>${usd(v.midpoint)}</strong>.</li><li>Sort the ${v.n} individual listing midpoints. Median midpoint = <strong>${usd(v.median_midpoint)}</strong>.</li></ol>`).join('');
  const br=[];
  for(const [key,c] of Object.entries(d.campuses)) for(const [inner,outer,label] of [[0,1,'0–1 mi'],[1,3,'>1–3 mi'],[3,5,'>3–5 mi'],[0,5,'Pooled 0–5 mi']]) {
    const set=A.campus(rows,key,inner,outer), v=A.summarize(set);
    for(const [type,a] of [['Room / bedroom',v.perOverall],['Whole unit',v.wholeOverall]]) br.push([esc(c.label),esc(label),type,set.length,a.n,v.excluded,usd(a.low_sum),usd(a.high_sum),usd(a.avg_low),usd(a.avg_high),usd(a.midpoint)]);
  }
  document.getElementById('band-math').innerHTML=table(['Campus','Distance','Category','Total listings','Prices used','Excluded listings','Low sum','High sum','Low mean','High mean','Mean midpoint'],br);
  const selections=[['Primary reviewed sample',u],['Omit all-2027-or-later offers',u.filter(r=>!r.all_2027_or_later)],['Omit waitlist mentions',u.filter(r=>!r.flags.includes('waitlist_mentioned'))],['Omit flagged Centennial Ridge endpoint',u.filter(r=>r.site_id!=='7e6kx1w')],['Apply all three omissions',u.filter(r=>!r.all_2027_or_later&&!r.flags.includes('waitlist_mentioned')&&r.site_id!=='7e6kx1w')]];
  document.getElementById('sensitivity-math').innerHTML=table(['Scenario','Room prices used','Room mean midpoint','Room median','Whole-unit prices used','Whole-unit mean midpoint'],selections.map(([name,set])=>{const v=A.summarize(set);return [name,v.perOverall.n,usd(v.perOverall.midpoint),usd(v.perOverall.median_midpoint),v.wholeOverall.n,usd(v.wholeOverall.midpoint)];}));
  document.getElementById('review-log').innerHTML=table(['ID / original listing','Original basis','Reviewed basis','Price status','Reason / evidence'],rows.filter(r=>!r.eligible_price||r.pricing_type!==r.original_pricing_type||r.flags.includes('high_endpoint_review')).map(r=>[`<a href="${esc(r.listing_url)}">${esc(r.site_id)} · ${esc(r.name)}</a>`,esc(r.original_pricing_type),esc(r.pricing_type),esc(r.review_status),esc(r.review_note)]));
  document.getElementById('budget-worked').innerHTML=`<strong>Default article example:</strong> ${usd(s.perOverall.midpoint)} room mean midpoint ÷ $1,300.00 monthly gross pay × 100 = <strong>${pct(s.perOverall.midpoint/1300*100)}</strong> at $15/hour, 20 hours/week, 52 paid weeks and 12 months. At $7.25/hour the same calculation is <strong>${pct(s.perOverall.midpoint/(7.25*20*52/12)*100)}</strong>. The room median is ${usd(s.perOverall.median_midpoint)}, giving ${pct(s.perOverall.median_midpoint/1300*100)} at $15/hour. Additional housing costs and support default to zero.`;
  const aidRows=[];
  for(const wage of [7.25,15]) for(const [name,grant,loan] of [['Work only',false,false],['Work + grant',true,false],['Work + loan',false,true],['Work + both',true,true]]) {
    const r=A.resources({wage,hours:20,...A.aidPreset({includeGrant:grant,includeLoan:loan})});
    aidRows.push([`$${wage}/hour`,name,usd(r.pay),usd(r.grant),usd(r.loan),usd(r.total),pct(s.perOverall.midpoint/r.total*100)]);
  }
  document.getElementById('aid-presets-math').innerHTML=table(['Wage at 20 hours/week','Checkbox selection','Monthly pay','Grant equivalent','Borrowed funds','Total resources','Room mean ÷ resources'],aidRows);
  document.getElementById('report-wording').innerHTML=`<strong>A claim this data supports:</strong> “Among ${s.perOverall.n} eligible room/per-bedroom advertisements in the September 9 snapshot within five straight-line miles of at least one of the three NC State reference points, the mean listing midpoint was ${usd(s.perOverall.midpoint)} and the median was ${usd(s.perOverall.median_midpoint)}. The mean was ${pct(s.perOverall.midpoint/1300*100)} of gross pay in a modeled $15/hour, 20-hour/week, year-round work scenario, before adding any unlisted housing costs.” This describes the reviewed portal sample and a scenario; it does not measure the percentage of students who can afford housing.`;
})();
